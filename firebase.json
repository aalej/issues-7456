{
  "firestore": [
    {
      "database": "(default)",
      "rules": "firestore.default.rules",
      "indexes": "firestore.default.indexes.json"
    },
    {
      "database": "named-db-4",
      "rules": "firestore.named-db.rules",
      "indexes": "firestore.named-db.indexes.json"
    }
  ]
}
